{
  "name": "gtray",
  "productName": "GTray",
  "version": "0.3.1",
  "description": "Minimal macOS Gmail wrapper: all your inboxes, the web experience intact",
  "author": "Mat\u00edas S\u00e1nchez Cabrera",
  "private": true,
  "main": "dist/main/main.js",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "start": "npm run build && electron .",
    "dist": "npm run build && CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --mac"
  },
  "build": {
    "appId": "app.gtray.mac",
    "productName": "GTray",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "assets/**/*",
      "package.json"
    ],
    "asar": true,
    "mac": {
      "category": "public.app-category.productivity",
      "icon": "assets/icon.icns",
      "target": "dir",
      "artifactName": "GTray-${arch}.${ext}",
      "extendInfo": {
        "NSBluetoothAlwaysUsageDescription": "GTray uses Bluetooth for security keys when signing in to Google.",
        "NSCameraUsageDescription": "GTray needs camera access for Google Meet video calls.",
        "NSMicrophoneUsageDescription": "GTray needs microphone access for Google Meet calls."
      }
    }
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "electron": "^43.0.0",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3"
  }
}